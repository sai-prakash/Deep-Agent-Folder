const key=process.env.OPENROUTER_API_KEY;if(!key)throw new Error('Set OPENROUTER_API_KEY');const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','X-Title':'AI Engineering Mastery Lab'},body:JSON.stringify({model:'openrouter/free',messages:[{role:'user',content:'Give one testable hypothesis for an agent-tool failure.'}]})});if(!r.ok)throw new Error(await r.text());const d=await r.json();console.log(d.choices[0].message.content);
